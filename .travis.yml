language: python
jobs:
  include:
    - name: "Linux"
      python: 3.9
    - name: "MacOS"
      os: osx
      osx_image: xcode12.2
      language: shell
install:
  - pip3 install -r requirements.txt
script:
  - pytest
